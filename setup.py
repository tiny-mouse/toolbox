from setuptools import setup

setup(
    name='toolbox',
    version='0.1-snapshot',
    packages=[
        'toolbox',
    ],
    namespace_packages = ['toolbox'],
    install_requires=[
    ],
    
    zip_safe=False,

    description = "Toolbox",
)

